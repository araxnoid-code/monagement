fn main() {
    // let target: u32 = 7;
    // let minimum = 2; // 2^2
    // let fl = 31 - target.leading_zeros();

    // let fl_idx = fl.saturating_sub(minimum);
    // let sl_idx = (target ^ (1 << fl)) >> (fl - minimum);

    // println!("fl_{} sl_{}", fl_idx, sl_idx);
}
